// create-student.dto.ts — DTO (PLACEHOLDER)

/*
import { IsString, IsEmail, IsInt, Min, IsNotEmpty, MaxLength } from 'class-validator';

export class CreateStudentDto {
  @IsString()
  @IsNotEmpty()
  @MaxLength(100)
  name: string;

  @IsEmail()
  @MaxLength(150)
  email: string;

  @IsInt()
  @Min(1)
  age: number;
}
*/

export { };
